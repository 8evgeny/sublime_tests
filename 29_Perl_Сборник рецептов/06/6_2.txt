Пример 6.2. killtags
#!/usr/bin/perl
# killtags – очень плохое удаление тегов HTML
undef $/;           # При каждом чтении загружается весь файл
while (<>) {        # Читать по одному файлу
    s/<.*?>//gs;    # Удаление тегов (очень скверное)
    print;          # Вывод в STDOUT
}


Пример 6.8. prime-pattern
#!/usr/bin/perl
# prime_pattern – разложение аргумента на простые множители по шаблону
for ($N = ('o' x shift); $N =~ /^(oo+?)\1+$/; $N =~ s/$1/o/g) {
    print length($1), " ";
}
print length ($N), "\n";

